{
  "name": "modulifier",
  "private": true,
  "devDependencies": {
    "gulp": "^3.9.0",
    "gulp-autoprefixer": "^2.3.1",
    "gulp-concat": "^2.5.2",
    "gulp-cssnano": "^1.0.0",
    "gulp-cssnext": "^1.0.1",
    "gulp-rename": "^1.2.2",
    "gulp-replace": "^0.5.3",
    "gulp-uglify": "^1.2.0"
  },
  "scripts": {
    "gulp": "gulp",
    "server": "jekyll serve --incremental",
    "build-watch": "gulp watch",
    "build": "gulp build",
    "deploy": "rsync --archive --recursive --delete --delete-excluded --progress --compress --chmod=u=rwX,go=rX ./_site/ keycdn:zones/modulifier/",
    "build-n-deploy": "npm run build && jekyll build && npm run deploy"
  }
}
